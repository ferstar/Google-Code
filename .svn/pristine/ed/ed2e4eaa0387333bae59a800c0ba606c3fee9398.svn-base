#!/bin/bash

CMD=`/bin/mount | /bin/grep 'on / '`
PERMS=`echo $CMD | awk ' { print $6 } '`
if [[ $PERMS -eq '(rw)' ]] ; then
    echo "ROOT_RO OK - $CMD (VM has not wet the bed yet)"
    exit 0
else
    echo "ROOT_RO CRITICAL - $CMD (VM wet the bed)"
    exit 2
fi
