#!/bin/bash

#/sbin/service gmond status > /dev/null 2>&1
RES=`ps -ef | grep gmond | grep -v grep | grep -v check | wc -l`

if [[ $RES -gt 0 ]] ; then
    echo "gmond OK"
    exit 0
fi
echo "gmond CRIT - gmond is not running"
exit 2


